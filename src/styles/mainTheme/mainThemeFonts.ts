import "@fontsource/karla";
import { DefaultThemeFonts } from "../types";

const mainThemeFonts: DefaultThemeFonts = {
  main: {
    family: "Karla, sans-serif",
    sizes: {
      display: "3rem",
      headline: "1.5rem",
      title: "1.25rem",
      body: "0.875rem",
    },
    weights: {
      bold: 800,
      regular: 400,
    },
  },
};

export default mainThemeFonts;
